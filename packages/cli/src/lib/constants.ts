/**
 * Semantic version range for the minimum supported React Native version.
 * @example "~0.72.0" indicates any version of React Native from 0.72.0 up to, but not including, 0.73.0.
 */
export const REACT_NATIVE_VERSION_RANGE = '~0.72.0';

/**
 * Semantic version range for the minimum supported React version.
 * @example "^18.0.0" indicates any version of React from 18.0.0 up to, but not including, 19.0.0.
 */
export const REACT_VERSION_RANGE = '^18.0.0';

// Event descriptions for different groups.

// Event for the 'template' group.
export const TEMPLATE_EVENT = 'Generating directories';

// Event for the 'env' group.
export const ENV_EVENT = 'Generating environments';

// Event for the 'code' group.
export const CODE_EVENT = 'Generating native code';

// Event for the 'dependencies' group.
export const DEPENDENCIES_EVENT = 'Installing native dependencies';
