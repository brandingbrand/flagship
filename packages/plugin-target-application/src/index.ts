import { summary } from "@brandingbrand/code-core";

const ios = summary.withSummary(
  async () => {
    //
  },
  "plugin-target-application",
  "platform::ios"
);

const android = () => {
  //
};

export { ios, android };
