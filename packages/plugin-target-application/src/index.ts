import { summary } from "@brandingbrand/kernel-core";

const ios = summary.withSummary(
  async () => {
    //
  },
  "plugin-target-application",
  "platform::ios"
);

const android = () => {
  //
};

export { ios, android };
