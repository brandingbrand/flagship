#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

if [ -t 1 ]; then
  exec </dev/tty
fi

./node_modules/.bin/commitlint -e $1
./tools/verify-commit.ts $1
./node_modules/.bin/npm-run-all -s test:affected lint:affected format:check workspace-lint
