import {ImageRequireSource} from 'react-native';

export default {
  account: require('./tabAccount.png') as ImageRequireSource,
  bag: require('./tabBag.png') as ImageRequireSource,
  home: require('./tabHome.png') as ImageRequireSource,
  shop: require('./tabShop.png') as ImageRequireSource,
};
