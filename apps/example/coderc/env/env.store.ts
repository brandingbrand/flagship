import type {AppEnvironment} from '@brandingbrand/code-app-env';
import {defineEnv} from '@brandingbrand/code-cli-kit';

export default defineEnv<AppEnvironment>({
  id: 'lmn34567',
  domain: 'https://storeDomain.com',
});
