{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "repo": {
      "type": "string",
      "description": "The repo where to push the filtered tree to"
    },
    "branch": {
      "type": "string",
      "default": "main",
      "description": "The branch where to ship the filtered tree to"
    },
    "dryRun": {
      "type": "boolean",
      "description": "Wether a push should be done when complete"
    },
    "maxWarnings": {
      "type": "integer",
      "description": "The maximum number of warnings to show"
    }
  },
  "required": ["repo"]
}
