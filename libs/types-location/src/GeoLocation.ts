/**
 * A set of coordinates specifying a location.
 */
export interface GeoLocation {
  /**
   * The latitude of the location in decimal degrees.
   */
  latitude: number;

  /**
   * The longitude of the location in decimal degrees.
   */
  longitude: number;
}
