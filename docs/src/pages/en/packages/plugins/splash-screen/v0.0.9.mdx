---
title: Plugin Splash Screen
description: brandingbrand/kernel-plugin-splash-screen
layout: ../../../../../layouts/docs.astro
lang: en
---

import CodeCopy from "../../../../../components/code-copy.astro";

## Plugin Splash Screen

`@brandingbrand/kernel-plugin-splash-screen` is a package that configures all the necessary native code for a native splash screen.

### Getting Started

<div class="flex w-full my-6">
  <CodeCopy args="yarn add -D @brandingbrand/kernel-plugin-splash-screen" />
</div>

Add the above noted package and the plugin is avaialble in the Kernel plugin ecosystem.

### Usage

After adding this plugin as a dev dependency it is necessary to add it to the kernel plugins configuration via the `package.json`.

`package.json`

```
  "kernel": {
    "plugins": [
      "@brandingbrand/kernel-plugin-splash-screen"
    ]
  },
```

Your Kernel environment should also be updated with the `@brandingbrand/kernel-plugin-splash-screen` required props from the defined interface.

`.kernelrc/env/env.<env>.ts`

```
  kernelPluginSplashScreen: {
    kernel: {
      ios: {
        type: 'generated',
        generated: {
            logoPath: "assets/splash-screen/ios/generated/logo.png",
            size: 212,
            backgroundColor: "#ffffff"
        },
      },
      android: {
        type: 'generated',
        generated: {
            logoPath: "assets/splash-screen/ios/generated/logo.png",
            size: 180,
            backgroundColor: "#ffffff"
        },
      },
    },
  },
```

or

```
  kernelPluginSplashScreen: {
    kernel: {
      ios: {
        type: "legacy",
        legacy: {
          path: "assets/splash-screen/ios/legacy",
          xcassets: "LaunchImages.xcassets",
        },
      },
      android: {
        type: "legacy",
        legacy: {
          path: "assets/splash-screen/android/legacy",
        },
      },
    },
  },
```

The legacy splash screen option can be utilized if you have all the generated assets - this schema is preferred if you have a complex splash screen i.e. background images, off-centered offsets, etc.

The generated splash screen option can be used if you have a very simple splash screen with a background color and a centered logo.

The interface in your `.kernelrc/env/env.<env>.ts` has the following props:

<div class="my-4 overflow-scroll">
| attribute                                | description                                  |
| :--------                                | :----------                                  |
| kernel.ios.type                          | generated or legacy type                     |
| kernel.ios.legacy.path                   | path to the legacy assets                    |
| kernel.ios.legacy.xcassets               | name, including extension, of xcode xcaseets |
| kernel.ios.generated.logoPath            | path to logo for generated option            |
| kernel.ios.generated.size                | size of logo to be generated                 |
| kernel.ios.generated.backgroundColor     | background color of splash screen            |
| kernel.android.type                      | generated or legacy type                     |
| kernel.android.legacy.path               | path to legacy assets                        |
| kernel.android.generated.logoPath        | path to logo for generated option            |
| kernel.android.generated.size            | size of logo to be generated                 |
| kernel.android.generated.backgroundColor | background color of splash screen            |
</div>

### Changelog

[**dfcefeb9d**](https://github.com/brandingbrand/flagship/pull/2508/commits/dfcefeb9d32eabe878bfd0a9ae0729c441d8d6c5) -- No change
